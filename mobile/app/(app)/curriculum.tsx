import { FeaturePlaceholder } from '../../src/components/FeaturePlaceholder';

export default function CurriculumScreen() {
  return (
    <FeaturePlaceholder
      title="Curriculum"
      description="Curriculum flow is mapped. Next migration step ports LearningModuleRenderer and module interaction logic from the web app."
    />
  );
}
