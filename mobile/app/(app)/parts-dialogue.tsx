import { FeaturePlaceholder } from '../../src/components/FeaturePlaceholder';

export default function parts_dialogueScreen() {
  return (
    <FeaturePlaceholder
      title="Parts Dialogue"
      description="AI/voice dialogue migration pending (proxy + native voice)."
      therapistOnly={false}
    />
  );
}
