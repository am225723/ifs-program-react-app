import { FeaturePlaceholder } from '../../src/components/FeaturePlaceholder';

export default function diagnosticScreen() {
  return (
    <FeaturePlaceholder
      title="PIN/Auth Diagnostic"
      description="Legacy diagnostic screen retained as debug placeholder."
      therapistOnly={true}
    />
  );
}
