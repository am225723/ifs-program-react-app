import { FeaturePlaceholder } from '../../src/components/FeaturePlaceholder';

export default function adminScreen() {
  return (
    <FeaturePlaceholder
      title="Admin Dashboard"
      description="Admin dashboard migration pending."
      therapistOnly={true}
    />
  );
}
