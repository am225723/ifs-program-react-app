import { FeaturePlaceholder } from '../../src/components/FeaturePlaceholder';

export default function auth_debugScreen() {
  return (
    <FeaturePlaceholder
      title="Auth Debug"
      description="Auth debug utilities migration pending."
      therapistOnly={true}
    />
  );
}
