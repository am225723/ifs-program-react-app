import { FeaturePlaceholder } from '../../src/components/FeaturePlaceholder';

export default function profileScreen() {
  return (
    <FeaturePlaceholder
      title="Profile"
      description="Profile summary and export migration pending."
      therapistOnly={false}
    />
  );
}
