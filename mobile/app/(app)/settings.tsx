import { FeaturePlaceholder } from '../../src/components/FeaturePlaceholder';

export default function SettingsScreen() {
  return (
    <FeaturePlaceholder
      title="Settings"
      description="Theme/preferences migration is pending. This route is active and ready for settings form porting."
    />
  );
}
